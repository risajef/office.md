# Notes

This is a separate Markdown file included by the feature tour.

Change this paragraph and return to `feature-tour.md` to see the live preview
refresh. The include keeps a file relationship instead of copying the text.


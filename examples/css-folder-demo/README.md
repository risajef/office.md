# CSS theme demo

Open this folder with the **CSS folder** button. Every CSS file appears in the
workspace tree and can be applied directly. The files also become selectable
themes in the Theme menu.
